# Carlo shell behaviour is installed by scripts/05-setup-dotfiles.sh as managed
# blocks. This file is retained as a concise reference only; it is never copied
# wholesale over an existing ~/.zshrc.

# The installed runtime block owns Homebrew, nvm-selected Node, ~/.local/bin,
# and the bootstrap-owned uv environment. Projects should use their own Python
# environments; the baseline deliberately provides no venv helper.

# The Carlo shell block loads Oh My Zsh with the robbyrussell theme and git
# plugin, and selects `zed --wait` only when the Zed CLI is available.
