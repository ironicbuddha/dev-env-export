# Claude Plugin Catalogue

No Claude plugins are installed or enabled by this bootstrap. Prefer a CLI or
selected Skill Hub skill whenever it can provide the capability.

If a project needs a Claude plugin, install it deliberately in Claude Code and
keep its authentication, trust state, and plugin runtime data local. Context7
should use its CLI or Skill Hub skill path rather than a default plugin.
